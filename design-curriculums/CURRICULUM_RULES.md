# LEARNING ACTIVITIES
`viewFlashcards`: `practiceMinutes` is calculated deterministically with [ROUND_UP(number of items in `vocabList` x 15s)]
`speakFlashcards`: `practiceMinutes` is calculated deterministically with [ROUND_UP(number of items in `vocabList` x 15s)]