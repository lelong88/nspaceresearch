This is an evergreen campaign for a fixed subscriber list that automatically includes all existing and new users in `my_user` table in db.

The `email_list` is: 4 (Coaching)

Each user will have a completely personalized series of emails written by AI agent, and in-app banners produced by AI agent for EACH USER.

Each user will have all the content in the folder that is their uid. the test user is: `zs5AMpVfqkcfDf8CJ9qrXdH58d73`. all of previous sent emails and previously sent in-app banners are in db that you have access to via mcp postgres (free style read-only sql) and the cloudflare worker api endpoints.

The goal of this email_list and evergreen campaign is to deliver educational content, learning nudges, learning statistics, learning diagnosis, etc. at the right time with the right content in the right place (email vs. in-app banners etc.)